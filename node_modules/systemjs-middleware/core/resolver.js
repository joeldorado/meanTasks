"use strict";
var ResolverNull = (function () {
    function ResolverNull() {
    }
    Object.defineProperty(ResolverNull.prototype, "name", {
        get: function () {
            return "null";
        },
        enumerable: true,
        configurable: true
    });
    ResolverNull.prototype.resolve = function (location, logger) {
        logger.log(location);
        return Promise.resolve(location);
    };
    return ResolverNull;
}());
exports.ResolverNull = ResolverNull;
